version: '3.3'

services:

  mysql-test-service:
    container_name: mysql-test-service
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'vfrfhfirf'
    ports:
      - 3307:3306
